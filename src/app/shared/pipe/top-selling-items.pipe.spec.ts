import { TopSellingItemsPipe } from './top-selling-items.pipe';

describe('TopSellingItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new TopSellingItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
